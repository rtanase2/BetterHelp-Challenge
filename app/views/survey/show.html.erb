<!DOCTYPE html>
<html>
<head>
    <title><%= @survey.name %></title>
</head>
<body>
    <h1> <%= @survey.name %> <a href=<%= "/survey/" << params[:id] << "/question" %>><button input="button">Edit</button></a> <a href= "/survey"><button input="button">Back</button></a> </h1>
    <% for q in @questions %>
        <h3><%= q.question %></h3>
        <% for a in @answers.where(:question_id => q.id) %>
            <p> <%= a.option %></p>
        <% end %>
    <% end %>
</body>
</html>
